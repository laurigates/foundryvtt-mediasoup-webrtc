// Vitest setup — runs before the unit suite (tests/unit/**).
// Foundry globals are not available under Node; unit tests should target pure
// logic (constants, helpers) or stub the globals they need. Add shared stubs
// here as the suite grows.

export {};
